<%= form_for(knowledge, url: knowledges_path, html: {method: "post"} ) do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Save' %>
  </p>
<% end %>
